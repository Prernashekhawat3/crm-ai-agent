{
  "name": "crm-ai-agent-root",
  "version": "1.0.0",
  "description": "Monorepo root for AI Customer Support Refund Agent",
  "scripts": {
    "db": "docker-compose up -d",
    "backend": "cd backend && PYTHONPATH=. .venv/bin/uvicorn app.main:app --port 8000",
    "frontend": "cd frontend && npm run dev",
    "dev": "npm run db && npx concurrently --names \"DB,API,WEB\" -c \"gray,magenta,cyan\" \"echo 'Postgres running...'\" \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
